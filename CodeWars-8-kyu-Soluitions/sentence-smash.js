function smash(words) {
  return words.length > 0 ? words.join(' ') : '';
}
//   if(words.length > 0) {
//     let res = words.join(' ');
//     return `${res}`;
//   } else return "";
// };
// smash (['add', 'world', 'dog']);
